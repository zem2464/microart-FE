import React from "react";
import { SettingOutlined } from "@ant-design/icons";

const TransferModeManagerAddIcon = (props) => <SettingOutlined {...props} />;

export default TransferModeManagerAddIcon;
